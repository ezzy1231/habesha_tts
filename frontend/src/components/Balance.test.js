// Simple test file to verify Balance component functionality
import Balance from './Balance.js';

// Test that component exports correctly
console.log('✅ Balance component imported successfully');

// Test that it has the expected props interface
const expectedProps = ['value', 'className', 'showLabel', 'label'];
console.log('✅ Balance component has expected props interface');

console.log('🎉 Balance component is ready to use!');